apiVersion: v1
kind: Service
metadata:
  name: clusterip-onboarding-cron-app
spec:
  type: ClusterIP
  selector:
    app: onboarding-cron-app
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081

