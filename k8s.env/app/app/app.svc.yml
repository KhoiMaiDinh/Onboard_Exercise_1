apiVersion: v1
kind: Service
metadata:
  name: clusterip-onboarding-app
spec:
  type: ClusterIP
  selector:
    app: onboarding-app
    network/kafka-network: "true"
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080